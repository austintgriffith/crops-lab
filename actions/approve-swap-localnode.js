// Action: approve-swap-localnode — approve-swap with Ethereum set to your node in MetaMask's UI
// first (see mm-localnode.js). Uses the warm wallet-profile.
// wallet: metamask
process.env.THEN = "approve-swap";
require("./mm-localnode.js");
