// Action: send-token-localnode — send-token with Ethereum set to your node in MetaMask's UI
// first (see mm-localnode.js). Uses the warm wallet-profile.
// wallet: metamask
process.env.THEN = "send-token";
require("./mm-localnode.js");
