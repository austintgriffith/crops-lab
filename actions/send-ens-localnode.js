// Action: send-ens-localnode — send-ens with Ethereum set to your node in MetaMask's UI
// first (see mm-localnode.js). Uses the warm wallet-profile.
// wallet: metamask
process.env.THEN = "send-ens";
require("./mm-localnode.js");
